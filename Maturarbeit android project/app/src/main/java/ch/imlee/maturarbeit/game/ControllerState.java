package ch.imlee.maturarbeit.game;

// the states are used to determine what kind of action the user is currently performing on the GameSurface.
public enum ControllerState {
    NULL, AIMING, FOCUS
}
