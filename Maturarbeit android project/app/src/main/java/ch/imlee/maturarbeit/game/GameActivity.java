package ch.imlee.maturarbeit.game;

import android.app.Activity;

public class GameActivity extends Activity {
/*
    Button particleButton;
    Button bombButton;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(new GameView(this));

        particleButton = (Button) findViewById(R.id.button_particle);
        bombButton = (Button) findViewById(R.id.button_bomb);
        particleButton.setOnClickListener(new View.OnClickListener(){
            public void  onClick(View v) {
            //save that p-button was clicked
            }
        });
        bombButton.setOnClickListener(new View.OnClickListener(){
             public void  onClick(View v) {
            //save that b-button was clicked
            }
        });
    }*/
}
