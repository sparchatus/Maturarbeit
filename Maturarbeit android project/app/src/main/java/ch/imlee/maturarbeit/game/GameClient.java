package ch.imlee.maturarbeit.game;


import android.app.Activity;
import android.os.Bundle;
import android.view.Display;

public class GameClient extends Activity {

    /**private int screenWidth;
    private int screenHeight;

    private GameSurface surface;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_capture_the_flag);
        Display display = getWindowManager().getDefaultDisplay();
        screenWidth = display.getWidth();
        screenHeight = display.getHeight();
        surface = (GameSurface) findViewById(R.id.capture_the_flag_surface);
        surface.setScreenDimensions(screenWidth, screenHeight);
    }
    */
}